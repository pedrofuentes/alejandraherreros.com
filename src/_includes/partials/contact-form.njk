{# Contact form (Web3Forms) — implemented by Track D.
   A static <form> POSTing to https://api.web3forms.com/submit using the
   access key in site.contact.web3formsAccessKey, with a honeypot field,
   a success/error state, and a graceful mailto: fallback when no key is set.
   Styles live in /assets/css/contact-form.css. #}
