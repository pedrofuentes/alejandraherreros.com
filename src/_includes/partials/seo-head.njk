{# SEO / meta tags — implemented by Track F.
   Should add: meta description, canonical URL, Open Graph + Twitter cards,
   favicon links, theme-color. Build on values from src/_data/site.json. #}
<meta name="description" content="{{ description or site.description }}" />
